<% if @user != nil %>
    <div class="span6">
        <h2>My projects <%= link_to "New project", new_project_path, :class => "btn btn-success btn-medium" %></h2>
        <table class="table table-striped table-hover">
            <% if @user.projects.empty? %>
                <tr><td>No projects</td></tr>
            <% else %>
                <% @user.projects.each do |project| %>
                    <tr>
                        <td><%= link_to project.title, project , :class =>  "" %>
                            <div class="pull-right">
                                <%= link_to "Edit", edit_project_path(project), :class => "btn btn-small" %>
                                <%= link_to "Destroy", project, :class => "btn btn-danger btn-small", :method => :delete, :data => { :confirm => 'Are you sure?' } %></a>
                            </div>
                        </td>
                    </tr>
                <% end %>
            <% end %>
        </table>
    </div>
<% else %>
    <%= link_to "Sign in with twitter", "/auth/twitter" %>
<% end %>




<br />
