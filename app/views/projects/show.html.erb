<p id="notice"><%= notice %></p>

<%= link_to '< Back to my projects', root_url, :class => "btn btn-small" %> | 
<%= link_to 'Edit project', edit_project_path(@project), :class => "btn btn-small" %>
<h1><%= @project.title %></h1>

<div class="row">
  <div class="span6">
    <h4>Issues</h4>
    <table class="table table-striped table-hover">
      <% @project.issues.each do |issue| %>
          <tr>
              <td><%= link_to issue.title, [@project, issue], :class =>  "" %>
                  <div class="pull-right">
                      <%= link_to "", edit_project_issue_path(issue), :class => "btn btn-small icon-pencil" %>
                      <%= link_to "", [@project, issue], :class => "btn btn-small btn-danger icon-trash", :method => :delete, :data => { :confirm => 'Are you sure?' } %></a>
                  </div>
              </td>
          </tr>
      <% end %>
    </table>
  </div>
  <div class="span5 offset1">
    <h4>Description:</h4>
    <%= @project.description %>

    <h4>Add issue</h4>
    <%= render 'issues/form' %>
  </div>
</div>

<!-- <%= render @project.issues %> -->
